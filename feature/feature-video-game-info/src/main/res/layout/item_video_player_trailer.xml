<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="@color/primary_background">

    <ImageView
        android:id="@+id/video_player_preview"
        android:layout_width="wrap_content"
        android:layout_height="250dp"
        android:layout_marginVertical="5dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:src="@drawable/video_game_background_image"
        tools:ignore="ContentDescription" />

    <ImageView
        android:id="@+id/play"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:src="@drawable/ic_play"
        app:layout_constraintStart_toStartOf="@+id/video_player_preview"
        app:layout_constraintEnd_toEndOf="@+id/video_player_preview"
        app:layout_constraintBottom_toBottomOf="@+id/video_player_preview"
        app:layout_constraintTop_toTopOf="@+id/video_player_preview"
        tools:ignore="ContentDescription" />

    <TextView
        android:id="@+id/video_player_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="5dp"
        android:text="@string/trailer_title"
        android:textStyle="bold"
        android:textSize="16sp"
        android:textColor="@color/white"
        android:textAlignment="textStart"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/video_player_preview"/>

</androidx.constraintlayout.widget.ConstraintLayout>